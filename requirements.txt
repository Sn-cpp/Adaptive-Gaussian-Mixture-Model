numpy

# Keep numba below 0.62 on Colab. From 0.62 the CUDA target lives in a separate
# `numba-cuda` package, and Colab's preinstalled one does not match a newer
# numba: `from numba import cuda` then raises AttributeError, `cuda.is_available()`
# never runs, and gmm/__init__.py quietly sets GMM_CUDA = None. The CUDA backend
# disappears with no error message. Measured on Colab with numba 0.67.
numba<0.62

opencv-python
matplotlib
pandas
tqdm
pytest

# graphcut/: the reference max-flow the Push-Relabel tests are scored against.
PyMaxflow

# benchmark.py only — reads keypresses to switch panels while it runs.
# On Linux it needs root; skip it if you are not running benchmark.py.
# keyboard

# Only needed for GMM_CUPY — pick the wheel matching your CUDA runtime,
# e.g. cupy-cuda12x on Colab.
# cupy-cuda12x
